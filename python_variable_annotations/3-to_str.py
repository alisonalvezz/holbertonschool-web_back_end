#!/usr/bin/env python3
"""
Function that takes a float and returns a str representation of it
"""


def to_str(n: float) -> str:
    """
    Takes a float and gives a str representation
    Attributes:
        n: the float to make the str representation
    Returns:
        A string representation of n
    """
    return str(n)
